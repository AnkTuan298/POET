<UserControl x:Class="POET.TestHistoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" 
        Style="{StaticResource BackButton}"
        Click="BtnBack_Click"
        Width="50"
        Height="30"
        HorizontalAlignment="Right"/>

        <Grid Margin="20" Grid.Row="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Test History"
                 Background="White"
                 FontSize="24" FontWeight="Bold"
                 Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" 
                x:Name="historyGrid"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                HeadersVisibility="Column"
                Margin="0,0,0,20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Test Name" Binding="{Binding TestName}" Width="*"/>
                <DataGridTextColumn Header="Score" Binding="{Binding Score}" Width="Auto"/>
                <DataGridTextColumn Header="Date Taken" Binding="{Binding DateTaken,StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        
    </Grid>
    </Grid>
</UserControl>