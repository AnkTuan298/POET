<UserControl x:Class="POET.Views.ViewStudentProgressPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:POET.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Student Progress" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <DataGrid x:Name="progressGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="0,10,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Student Name" Binding="{Binding StudentName}" Width="2*"/>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="2*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*"/>
                    <DataGridTextColumn Header="Test Name" Binding="{Binding TestName}" Width="2*"/>
                    <DataGridTextColumn Header="Latest Score" Binding="{Binding LatestScore}" Width="*"/>
                    <DataGridTextColumn Header="Date Taken" Binding="{Binding DateTaken}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <Button Content="Back" Width="120" Height="40" Margin="0,0,0,10"
                    Click="BtnBack_Click"
                    Background="DodgerBlue" Foreground="White" FontWeight="Bold"
                    VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>